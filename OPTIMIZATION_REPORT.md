# Auto-Tuner 竞品分析与优化报告

> 基于 Optuna、Ray Tune、W&B Sweeps、ml-intern 等顶级产品的深度对比分析

---

## 一、竞品全景

### 顶级调参工具对比

| 产品 | 类型 | 核心优势 | 粘性来源 | 价格 |
|------|------|----------|----------|------|
| **Optuna** | 开源框架 | Define-By-Run API、剪枝策略丰富、Dashboard 可视化 | 轻量 Pythonic，迁移成本低但生态深 | 免费 |
| **Ray Tune** | 分布式框架 | 跨集群分布式、ASHA/HyperBand/PBT 调度器 | Ray 生态锁定（Train/Data/Serve） | 开源+企业版 |
| **W&B Sweeps** | 实验管理平台 | 实验追踪 + HPO 一体、可视化 Dashboard 极强 | 团队实验历史积累，迁移痛苦 | 免费100GB/$50人/月 |
| **ml-intern** | AI Agent | 自然语言驱动、HF 生态深度集成、自主研究+编码 | 一站式 ML agent，从研究到部署 | 免费 |
| **Ax/BoTorch** | 研究级工具 | 高斯过程贝叶斯优化、多目标优化 | 研究团队的精细化控制需求 | 免费 |

### 关键洞察：用户为什么"回不去"

1. **数据引力**：W&B 积累了上千条实验记录，迁移成本极高
2. **生态锁定**：Ray Tune 用户已投入 Ray 基础设施，换工具意味着重建
3. **习惯成本**：Optuna 的 `study.optimize()` 一行代码搞定，用户懒得换
4. **可视化成瘾**：W&B Dashboard 成了团队的"唯一真相来源"
5. **AI 原生范式**：ml-intern 的"说人话→自动做"正在颠覆传统配置驱动模式

---

## 二、资深竞品用户视角：什么让我转投 auto-tuner？

### 我是 Optuna 资深用户

**Optuna 的痛点**：
- 需要写 Python 代码定义搜索空间和目标函数
- 调参策略需要自己选（TPE/CMA-ES/Grid），不懂的人选不好
- 没有自动的架构修改能力，只调超参不改架构
- 需要自己写训练循环，Optuna 只管参数采样

**auto-tuner 的吸引力**：
- 自然语言描述目标，不用写代码 → "dice >= 0.9" 就够了
- 自动识别可调参数，不用手动定义搜索空间
- 架构回溯能力（Optuna 完全没有）
- 全程自主，不用盯着看

**但 auto-tuner 缺什么让我犹豫**：
- 没有参数重要性分析（Optuna 的 fANOVA）
- 没有剪枝策略（提前砍掉烂配置）
- 没有可视化 Dashboard
- 没有实验历史对比（跨项目的）

### 我是 W&B Sweeps 资深用户

**W&B 的痛点**：
- 需要写 YAML 配置定义 sweep
- 需要自己写训练脚本适配 W&B API
- 贵（团队版 $50/人/月）
- 没有自动架构修改

**auto-tuner 的吸引力**：
- 零配置，自然语言驱动
- 免费（Claude Code 订阅内）
- 架构回溯 + 多源搜索

**但 auto-tuner 缺什么让我犹豫**：
- 没有实时可视化（看不到调参过程）
- 没有实验对比表（只能看最终报告）
- 没有团队协作（一个人用）
- 没有模型版本管理

### 我是 ml-intern 资深用户

**ml-intern 的痛点**：
- 通用 ML agent，不专注调参
- 调参逻辑需要用户自己引导
- 没有系统性的参数搜索策略

**auto-tuner 的吸引力**：
- 专注调参，流程更专业
- 有系统性的粗搜→细搜→微调策略
- 故障熔断机制更完善

**但 auto-tuner 缺什么让我犹豫**：
- 没有 ml-intern 的 HF 生态集成
- 没有云端沙箱执行能力
- 没有论文搜索能力（只搜 GitHub）

---

## 三、资深 auto-tuner 用户视角：新增什么功能让我"上瘾"？

### 功能 1：参数重要性分析（优先级：高）

**当前问题**：agent 盲目调所有参数，不知道哪些参数真正重要。

**期望行为**：每轮调参后自动计算参数重要性排名，固定不重要的参数，集中火力调重要的。

**实现建议**：在 step3-optimization.md 的趋势分析中加入参数重要性计算。方法：对已完成的实验做简单的方差分析或排列重要性。

**粘性效果**：用过参数重要性分析的用户，回到盲目调参会觉得"太蠢了"。

### 功能 2：提前终止烂配置（优先级：高）

**当前问题**：5 路并行，每路都跑到结束，即使某路第 1 轮就明显不行。

**期望行为**：类似 Optuna 的 Pruner，如果某配置跑了几轮后指标明显低于当前最佳，提前终止，省资源给新配置。

**实现建议**：在 step2-tuning.md 中加入"中途检查"机制——每个分支完成 20% 训练后检查趋势，明显不行就 kill 掉。

**粘性效果**：省 30-50% 的调参时间，用户用过就回不去。

### 功能 3：实验对比可视化（优先级：中）

**当前问题**：只有最终报告，调参过程中看不到全局对比。

**期望行为**：每轮结束后生成一个简单的对比表（Markdown 或 HTML），显示所有配置的指标对比、参数对比、趋势图。

**实现建议**：在 step2 的 progress.md 中加入对比表，或生成一个独立的 `experiment/comparison.html`。

**粘性效果**：可视化让调参过程"可感知"，用户更有掌控感。

### 功能 4：跨项目经验迁移（优先级：中）

**当前问题**：每次调参都从零开始，不记得之前项目的经验。

**期望行为**：记住之前调参的经验——哪些参数范围通常有效、哪些策略好用、哪些坑踩过。

**实现建议**：在 `experiment/` 同级维护一个 `~/.claude/skills/auto-tuner/experience/` 目录，每次调参结束后把关键经验写入。下次调参时先读经验。

**粘性效果**：越用越聪明，用户依赖积累的经验。

### 功能 5：自动代码修复（优先级：中）

**当前问题**：step1 说"代码有 bug 先修"，但没有具体的修复策略。

**期望行为**：检测到代码 bug 后，自动分析错误日志、定位问题、修复代码、验证修复，然后继续调参。

**实现建议**：在 step1 和 step2 的故障处理中加入自动修复流程（读错误日志 → grep 相关代码 → 修复 → 重跑）。

**粘性效果**：从"帮你调参"升级到"帮你搞定一切"。

### 功能 6：多目标优化（优先级：低）

**当前问题**：只支持单指标达标（如 dice >= 0.9）。

**期望行为**：支持多指标同时优化（如 dice 最大化同时推理时间最小化）。

**实现建议**：在达标定义中支持 Pareto 最优，报告中展示 Pareto 前沿。

**粘性效果**：解决更复杂的实际需求。

### 功能 7：云端执行支持（优先级：低）

**当前问题**：只能在本地跑，受本地 GPU 限制。

**期望行为**：支持把实验提交到云端（HF Jobs、AWS、GCP）执行。

**实现建议**：在 step2 的并行策略中加入"云端提交"选项。

**粘性效果**：突破本地资源限制，调参规模不受限。

---

## 四、优先级排序与实施路线

### P0（必做，直接影响核心价值）

| 功能 | 预期收益 | 实现难度 | 文件 |
|------|----------|----------|------|
| 参数重要性分析 | 减少无效搜索 30%+ | 低 | step3-optimization.md |
| 提前终止烂配置 | 省 30-50% 时间 | 中 | step2-tuning.md, failure-recovery.md |

### P1（应该做，提升体验显著）

| 功能 | 预期收益 | 实现难度 | 文件 |
|------|----------|----------|------|
| 实验对比可视化 | 调参过程可感知 | 低 | step2-tuning.md (progress.md 增强) |
| 跨项目经验迁移 | 越用越聪明 | 中 | 新增 references/experience.md |
| 自动代码修复 | 从调参到全栈 | 中 | step1, step2 故障处理 |

### P2（可以做，锦上添花）

| 功能 | 预期收益 | 实现难度 | 文件 |
|------|----------|----------|------|
| 多目标优化 | 解决复杂需求 | 高 | step1-planning.md, step3 |
| 云端执行 | 突破本地限制 | 高 | 新增 references/cloud-execution.md |

---

## 五、结论

auto-tuner 的核心差异化优势是**自然语言驱动 + 全程自主 + 架构回溯**，这是传统工具（Optuna/W&B/Ray Tune）完全不具备的。

要让用户"回不去"，关键是：
1. **做传统工具做不到的事**（架构回溯、自然语言目标、自动代码修复）— 已有
2. **把传统工具做得到的事做得更省心**（参数重要性、提前终止、可视化）— 待补
3. **越用越聪明**（经验迁移）— 待补

补上 P0 和 P1 的功能后，auto-tuner 将成为"会思考的调参专家"，而非"按流程跑的自动化脚本"。
