---
name: auto-tuner
description: >
  科研代码项目自动化调参 skill。适用于任何需要系统性调参的科研实验代码——深度学习训练、传统机器学习、数值模拟、图像处理 pipeline 等。
  当用户提到"调参"、"超参搜索"、"hyperparameter tuning"、"参数优化"、"跑实验"、"达标"、"dice"、"accuracy"、"loss"等关键词时触发。
  也适用于用户描述了一个实验目标（如"所有心脏部位 dice >= 0.9"）并希望 agent 自动迭代调参直到达标的情况。
  即使用户没有明确说"调参"，只要他们描述了一个需要反复实验才能达到指标的科研任务，也应触发此 skill。
---

# 科研代码自动化调参

你的目标是帮用户通过系统性参数搜索，让实验指标达到预设标准。**全程自主执行**，用户只在开头确认一次工作区，之后等最终结果即可。

流程：**工作区确认 → 项目理解与参数规划 → 并行调参 → 迭代优化（含架构回溯） → 达标报告**。

---

## 阶段零：确认工作区与权限配置

1. 从对话中提取项目路径，若没有则询问用户
2. 展示路径、顶层文件列表、是否 Git 仓库，等待用户确认
3. 确认后调用 `update-config` skill 配置该目录的完整权限（Bash/Read/Write/Edit/Glob/Grep 全部允许）
4. 自动检测工作区外的数据集/输出/环境路径，一并配置权限

**这是用户唯一需要参与的环节。** 之后所有决策由 agent 自主完成。

---

## 阶段一：项目理解、参数规划与达标定义

### 1.1 理解项目

从对话中提取项目目标、输入输出、评估指标、达标标准、硬件资源。信息不完整时只问缺失的部分，不要重复确认已有信息。如果用户没有现成代码，先写代码再继续。

### 1.2 数据集确认与记录

创建 `experiment/` 目录。然后对用户的数据集进行全面检查并保存记录到 `experiment/dataset_info.md`：

- **数据规模**：样本数量、训练/验证/测试集划分比例
- **数据格式**：文件类型（npy/nii/png/csv 等）、维度、分辨率
- **类别分布**：各类别样本数量、是否类别不平衡
- **数据质量**：是否有缺失值、异常值、损坏文件
- **标注情况**：标注格式、标注质量抽样检查
- **数据路径**：训练集/验证集/测试集的完整路径

这些信息会影响调参决策（如类别不平衡时需要调整 loss 函数或采样策略，数据量小时需要更强的数据增强），后续每轮分析时都会参考这份记录。

先跑一次 baseline 估算单轮耗时，将预估时间告知用户，并询问是否先用部分数据集（如 10%-20%）快速调参找到大致方向，再用全量数据精调。设置 10 分钟等待超时——如果用户未回复，默认使用全量数据集直接开始调参。如果用户同意用子集，前期用子集调参，最后 2-3 轮切换到全量数据验证。

### 1.3 自我辩论设计调参框架

根据项目特点，自行思考所有可能的调参策略（不限于常见方法，可以结合项目特性发明新策略），列出 2-3 种最合适的，各写一句话优劣，选最优方案。记录到 `experiment/decision_log.md`。

### 1.4 参数扫描与达标定义

扫描代码识别可调参数，按类别整理（核心参数、结构参数、数据参数、训练参数），列出当前值和建议范围。

从用户描述中提取达标标准。如果用户说不确定或没给出具体数字，根据项目类型、当前 baseline 指标、同类任务的业界水平自行推断合理的达标标准，并在 decision_log.md 中说明推断依据。格式：主要指标（必须全部满足）、次要指标（尽量满足）、终止条件。

---

## 阶段二：并行调参执行

### 2.1 环境准备

建立实验目录结构（`experiment/round-N/config-NNN/`）。检测 GPU 和内存资源上限。baseline 已在阶段一 1.2 中完成，此处直接进入调参循环。

### 2.2 并行策略

默认 5 路并行。Git 项目用 worktree 隔离，非 Git 项目复制到独立目录，轻量实验用后台进程+不同参数文件。

动态调整：GPU 显存 > 85% 减 1 路，< 50% 加 1 路（上限 8），OOM 的分支降低 batch_size 重试。

### 2.3 单轮流程

生成 N 组配置 → 并行运行 → 监控进度 → 收集结果 → 写入 `results.json`（格式见 `references/results_schema.md`）。

每轮结束后更新 `experiment/progress.md`，记录当前轮数、最佳指标、是否达标、下一步计划。用户随时可查看此文件了解进展。

---

## 阶段三：迭代优化

### 3.1 分析与决策

每轮结束后：排名 → 趋势分析（哪个方向在改善、哪些参数已收敛） → 围绕最佳缩小范围 → 生成下一轮配置。

### 3.2 递进策略

粗搜（大范围随机采样找方向） → 细搜（围绕最佳缩小范围） → 微调（只动 1-2 个参数）。

### 3.3 架构回溯机制

当**连续 10 轮**无明显提升（< 0.01）且距达标仍有差距（> 0.03）时触发：

**Step 1 — 存档**：把当前最优代码、参数、完整历史保存到 `experiment/checkpoints/checkpoint-vN/`。

**Step 2 — 诊断**：分析 loss 曲线判断瓶颈（过拟合/欠拟合/容量不足/感受野不够/梯度问题）。

**Step 3 — 自主选方案**：列出 2-3 种架构修改方案，自我辩论评估收益/风险/代价/兼容性，选最优解。记录辩论过程到 decision_log.md。

**Step 4 — 执行**：从存档复制代码，修改架构，保留最优超参作为起点，重新走粗搜→细搜→微调。

**Step 5 — 判定与回档**：新架构至少调满 **10 轮**再判定。更优则继续，变差则记录失败原因后回档。持平再给 5 轮机会。所有方案都无效则停止。

每次回档必须写 `experiment/failed_architectures.md`：改了什么、为什么不行、得到什么教训。避免重复踩坑。

### 3.4 放弃机制

满足以下**任一**条件时，自动停止实验并生成报告：

1. **架构穷尽**：尝试了 3 种以上不同架构，每种都调满 10 轮，最佳指标仍距目标 > 0.05
2. **总轮数上限**：跨所有架构累计 50 轮仍无突破
3. **长期停滞**：最近 20 轮（不论是否换过架构）最佳指标变化 < 0.005

放弃时生成的报告需额外说明：已尝试的所有方案、各方案最佳指标、放弃原因分析（目标是否定得太高、数据质量是否有问题、任务本身是否超出当前架构能力）。

---

## 阶段四：达标报告

达标或触发终止条件后，读取 `references/report_template.md` 按模板生成报告。报告需包含：最佳配置、最佳指标、调参历程（每轮关键发现）、参数敏感性分析、建议。如有架构变更，还需包含架构变更历史和失败方案汇总。

---

## 重要原则

1. **全程自主**：用户只确认工作区，之后所有决策由 agent 自主完成
2. **先跑通再调参**：代码有 bug 先修，不在坏代码上浪费时间
3. **记录一切**：每个决策写入 decision_log.md，每个失败写入 failed_architectures.md
4. **保守优先**：不确定时选风险最低的方案，后续可以迭代改进
